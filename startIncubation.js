import { updateUser } from './src/utils/db';

export const startIncubation = async (state, showNotification, eggId, type) => {
    // Placeholder logic - needs actual egg inventory logic
    showNotification("Inkubation gestartet!", "success");
};